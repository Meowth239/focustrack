version: '3.8'

services:
  bot:
    build: .
    container_name: focustrack-bot
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - BOT_TOKEN=${BOT_TOKEN}
    ports:
      - "5000:5000"